# OpenWorker 每日工作日志

- 日期：2026-08-25
- 更新时间：2026-08-25 17:13（Asia/Shanghai）
- 仓库：`Czzzzzq/openworker`
- 分支：`main`
- 当前提交：`05f95559`
- 状态：正式代码已提交并推送；本地运行状态与项目产物未提交

## 今日摘要

- 完成飞书（Feishu/Lark）原生双向消息连接器的最小端到端实现。
- 接入本地 API 费用统计、余额与峰谷计价设置界面。
- 为 OpenWorker 项目安装并启用 `ppt-master` 技能。
- 将本地 MCP 项目统一归档到 `.dev-state/mcp/`，避免与标准 Skill 目录混放。
- 使用 `ppt-master` 生成自然保护主题的学生演示文稿及其研究、设计和验证产物。
- 更新 Office 文档与飞书消息能力相关依赖及桌面端打包配置。

## 飞书连接器

- 新增飞书应用凭据配置、校验、长连接监听与运行时适配器注册。
- 支持私聊消息和群聊中 @机器人的消息进入 OpenWorker。
- 支持按原消息/话题上下文回复，区分消息 ID 与话题根 ID。
- 支持发送消息时使用 `app_id` 与 `app_secret` 组合凭据。
- 群聊 @机器人时可分页读取成员列表，并将受信任的成员名称与 `open_id` 作为模型上下文注入；单次最多读取 200 人。
- 保留本地允许列表校验，并在连接器目录、设置界面、品牌图标和 README 中加入飞书入口。
- 将 `lark-oapi>=1.7.3,<2` 加入 messaging 可选依赖，并将 `websockets` 限制为 `<16`；PyInstaller 配置同步收集 `lark_oapi`。

## 费用面板

- 新增费用统计、官方余额、费用设置与价格同步的本地 API。
- 前端 API 增加费用统计、余额和设置类型及请求方法。
- 设置页新增“费用”标签，展示当前会话及累计用量、预算和峰谷计价信息。

## 技能、MCP 与交付物

- `ppt-master` 已安装到 `.dev-state/skills/ppt-master`，`SKILL.md` 存在且技能未被禁用。
- `ali-mcp` 与 `douyinmcp-mcp` 已统一放到 `.dev-state/mcp/`；其中 `douyinmcp-mcp` 是 MCP Server 项目，不含 `SKILL.md`，不再作为标准 Skill 处理。
- 新增 Office 文档交付依赖组：`python-docx`、`openpyxl`、`python-pptx`、`reportlab`。
- 生成自然保护主题学生演示文稿：`projects/nature-protection-students_ppt169_20260825/exports/nature-protection-students_20260825_141323.pptx`。
- 项目目录同时保留研究资料、事实清单、设计规范、13 页 SVG 源稿、备份和验证记录。

## 验证结果

- `git diff --check`：通过，无空白错误；仅出现 Windows 工作区 LF/CRLF 提示。
- 飞书连接器测试：`11 passed`。
- 飞书连接器图标前端测试：`6 passed`。
- GUI 生产构建：通过，Vite 成功转换 393 个模块。
- 构建仍有既存优化提示：`api.ts` 同时被静态和动态导入，且部分产物大于 500 kB；不影响本次构建成功。

## 当前工作区与注意事项

- 已同步 `upstream/main` 的 12 个新提交与 `v0.2.0` 标签，并将 5 个本地提交重放到新版上游之上。
- 正式改动已提交为 `05f95559`（`Add native Feishu connector and cost settings UI`），共涉及 21 个文件、4538 行新增和 41 行删除。
- 因同步上游重写了本地提交历史，已使用 `--force-with-lease` 安全更新 `origin/main`；推送后本地与远端一致。
- 当前复核 `origin/main...main` 为 `0 0`，本地提交与远端仍保持一致。
- `.dev-state/` 是本地运行状态，包含技能、MCP、会话和配置数据，不应直接纳入版本控制。
- `tests/.costmeter-tmp/` 已不存在，无需再清理。
- 今日生成的演示文稿与 `projects/` 内容是否进入仓库，应在提交前单独确认。
